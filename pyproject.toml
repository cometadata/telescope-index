[project]
name = "telescope-index"
version = "0.1.0"
description = "Data pipeline for indexing arXiv works to Typesense"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ijson>=3.4.0.post0",
    "orjson>=3.11.4",
    "tqdm>=4.67.1",
    "typesense>=1.3.0",
]
